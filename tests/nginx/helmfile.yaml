---
# environments:
#   homerun-dev:
#     values:
#       - env/homerun-dev.yaml

repositories:
  - name: nginx-stable
    url: https://charts.bitnami.com/bitnami

releases:
  - name: nginx
    namespace: default
    chart: nginx-stable/nginx
    version: 15.5.0 # Update as needed
    values:
      - values/values.yaml.gotmpl
      # - service:
      #     type: {{ .Values.serviceType }}