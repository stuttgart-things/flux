---
apiVersion: v1
kind: Namespace
metadata:
  name: ${VAULT_NAMESPACE:-vault}
  labels:
    toolkit.fluxcd.io/tenant: sthings-team
---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: stuttgart-things
  namespace: ${VAULT_NAMESPACE:-minio}
spec:
  type: oci
  interval: 1h
  url: oci://ghcr.io/stuttgart-things
