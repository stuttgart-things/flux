---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: homerun2-light-catcher
  namespace: ${HOMERUN2_NAMESPACE:-homerun2}
spec:
  interval: 1h
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: OCIRepository
    name: homerun2-light-catcher-kustomize
  prune: true
  wait: true
  targetNamespace: ${HOMERUN2_NAMESPACE:-homerun2}
  patches:
    # Remove the Ingress from the kustomize base (we use Gateway API HTTPRoute)
    - target:
        kind: Ingress
        name: homerun2-light-catcher
      patch: |
        apiVersion: networking.k8s.io/v1
        kind: Ingress
        metadata:
          name: homerun2-light-catcher
        $patch: delete
    # Remove the KCL-generated HTTPRoute (we provide our own via the component)
    - target:
        kind: HTTPRoute
        name: homerun2-light-catcher
      patch: |
        apiVersion: gateway.networking.k8s.io/v1
        kind: HTTPRoute
        metadata:
          name: homerun2-light-catcher
        $patch: delete
    # Override the container image tag
    - target:
        kind: Deployment
        name: homerun2-light-catcher
      patch: |
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: homerun2-light-catcher
        spec:
          template:
            spec:
              containers:
                - name: homerun2-light-catcher
                  image: ghcr.io/stuttgart-things/homerun2-light-catcher:${HOMERUN2_LIGHT_CATCHER_VERSION:-v1.1.0}
    # The base's placeholder Secret. sops/ or eso/ beside this component
    # creates the real one, so the credential never lives in this object.
    - target:
        kind: Secret
        name: homerun2-light-catcher-redis
      patch: |
        apiVersion: v1
        kind: Secret
        metadata:
          name: homerun2-light-catcher-redis
        $patch: delete
    # Override Redis connection and WLED endpoint
    - target:
        kind: Deployment
        name: homerun2-light-catcher
      patch: |
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: homerun2-light-catcher
        spec:
          template:
            spec:
              containers:
                - name: homerun2-light-catcher
                  env:
                    - name: REDIS_ADDR
                      value: redis-stack.${HOMERUN2_NAMESPACE:-homerun2}.svc.cluster.local
                    - name: REDIS_PORT
                      value: "6379"
