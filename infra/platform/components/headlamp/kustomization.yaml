---
# headlamp
#
# REQUIRES ../cilium-gateway in the same spec.components list, which in turn
# requires ../cilium-lb.
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ks-headlamp.yaml
